<div class="mt-2 mb-1">
  <div class="tooltip w-full" data-tip="Unlock with Enterpise">
    <%= link_to submitter.sent_at? ? 'Re-send SMS' : 'Send SMS', Uvtsign.multitenant? ? console_redirect_index_path(redir: "#{Uvtsign::CONSOLE_URL}/plans") : "#{Uvtsign::CLOUD_URL}/sign_up?#{{ redir: "#{Uvtsign::CONSOLE_URL}/on_premise" }.to_query}", class: 'btn btn-sm btn-primary text-gray-400 w-full' %>
  </div>
</div>
